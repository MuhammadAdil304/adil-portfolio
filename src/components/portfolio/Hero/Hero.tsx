import { ArrowForward, WorkOutlined } from "@mui/icons-material";
import { Box, Button, Chip, Container, Typography } from "@mui/material";
import { motion } from "framer-motion";
import { heroActions, heroHighlights } from "../../../data/hero";
import { heroStyles } from "./Hero.styles";
const heroImg = "/hero_img.png";
export const Hero = () => { const name="Muhammad Adil", role="Full Stack Developer", intro="I build clean, fast, and production-ready developer portfolios with a focus on clarity, usability, and strong execution."; return <Box component="section" id="top" sx={heroStyles.root}><Container maxWidth="lg" sx={heroStyles.container}><Box component={motion.div} initial={{opacity:0,y:24}} animate={{opacity:1,y:0}} transition={{duration:.5}}><Typography variant="overline" color="primary.light" sx={heroStyles.overline}>Hi, I am</Typography><Typography variant="h3" sx={heroStyles.name}>{name}</Typography><Typography variant="h5" color="text.secondary" sx={heroStyles.role}>{role}</Typography><Typography variant="body1" color="text.secondary" sx={heroStyles.intro}>{intro}</Typography><Box sx={heroStyles.actions}>{heroActions.map((link)=><Button target="_blank" key={link.label} href={link.href} variant={link.variant === "primary" ? "contained" : "outlined"} endIcon={link.variant === "primary" ? <ArrowForward/> : <WorkOutlined/>}>{link.label}</Button>)}</Box><Box sx={heroStyles.highlights}>{heroHighlights.map((item)=><Chip key={item} label={item}/>)}</Box></Box><Box sx={heroStyles.imageWrap}><motion.img src={heroImg} alt="my picture" style={heroStyles.image as object} whileHover={{scale:1.05}} whileTap={{scale:.95}} /></Box></Container></Box>; };
